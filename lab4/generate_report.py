"""
Модуль для генерации полного Markdown отчета с визуализацией
"""
import time
import logging
from pathlib import Path
from typing import Dict, List, Tuple, Any, Optional
from datetime import datetime

from models import Portfolio, Scenario
from solver import DynamicProgrammingSolver
from data_loader import (
    load_from_excel, validate_scenarios, load_commissions,
    initialize_portfolio
)
from path_recovery import monte_carlo_simulation, recover_path
from visualizers import (
    visualize_initial_portfolio, visualize_scenarios, visualize_dp_process,
    visualize_optimal_actions, visualize_portfolio_evolution, visualize_monte_carlo,
    visualize_sensitivity, visualize_criteria_comparison, visualize_3d_value_function,
    visualize_decision_tree
)
from data_exporter import export_all_data
from sensitivity_analysis import run_full_sensitivity_analysis
from criteria_comparison import compare_all_criteria
from constants import (
    INITIAL_CB1, INITIAL_CB2, INITIAL_DEP, INITIAL_CASH,
    USE_COMMISSIONS, USE_MIN_CONSTRAINTS, NUM_STAGES,
    MIN_CB1, MIN_CB2, MIN_DEP
)

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')
logger = logging.getLogger(__name__)


def create_report_structure() -> None:
    """
    Создает структуру папок для отчета
    """
    try:
        Path('output').mkdir(exist_ok=True)
        Path('output/diagrams').mkdir(exist_ok=True)
        Path('output/data').mkdir(exist_ok=True)
        logger.info("✓ Структура папок создана")
    except Exception as e:
        logger.error(f"Ошибка при создании структуры папок: {e}")
        raise


def generate_all_visualizations(solver: DynamicProgrammingSolver,
                               initial_portfolio: Portfolio,
                               scenarios: Dict[int, List[Scenario]],
                               commissions: Dict[str, float],
                               path: List[Portfolio],
                               actions: List[Tuple[float, float, float]],
                               mc_results: Dict[str, Any],
                               sensitivity_results: Dict[str, List[Dict[str, float]]],
                               criteria_results: Dict[str, float],
                               timing_data: Dict[int, float] = None) -> Dict[str, str]:
    """
    Генерирует все визуализации
    
    Args:
        solver: Решатель с решенной задачей
        initial_portfolio: Начальный портфель
        scenarios: Сценарии
        commissions: Комиссии
        path: Траектория портфеля
        actions: Оптимальные действия
        mc_results: Результаты Monte Carlo
        sensitivity_results: Результаты анализа чувствительности
        criteria_results: Результаты сравнения критериев
        timing_data: Данные о времени выполнения
        
    Returns:
        Словарь с путями к созданным файлам
    """
    logger.info("Генерация всех визуализаций...")
    diagram_paths = {}
    
    try:
        # 1. Начальный портфель
        diagram_paths['initial_portfolio'] = visualize_initial_portfolio(
            initial_portfolio, commissions
        )
        
        # 2. Сценарии
        heatmap_path, tree_path = visualize_scenarios(scenarios)
        diagram_paths['scenarios_heatmap'] = heatmap_path
        diagram_paths['scenarios_tree'] = tree_path
        
        # 3. Процесс DP
        diagram_paths['dp_process'] = visualize_dp_process(solver, timing_data)
        
        # 4. Оптимальные действия
        expected_values = [solver.value_function[1].get(
            solver._find_closest_state(initial_portfolio, 1), 0.0
        )] if 1 in solver.value_function else None
        diagram_paths['optimal_actions'] = visualize_optimal_actions(actions, expected_values)
        
        # 5. Эволюция портфеля
        diagram_paths['portfolio_evolution'] = visualize_portfolio_evolution(path)
        
        # 6. Monte Carlo
        dist_path, cdf_path = visualize_monte_carlo(mc_results)
        diagram_paths['mc_distribution'] = dist_path
        diagram_paths['mc_cdf'] = cdf_path
        
        # 7. Чувствительность
        if sensitivity_results:
            prob_path, comm_path = visualize_sensitivity(sensitivity_results)
            diagram_paths['sensitivity_prob'] = prob_path
            diagram_paths['sensitivity_comm'] = comm_path
        
        # 8. Сравнение критериев
        if criteria_results:
            diagram_paths['criteria_comparison'] = visualize_criteria_comparison(criteria_results)
        
        # 9. 3D функция ценности
        diagram_paths['3d_value'] = visualize_3d_value_function(solver)
        
        # 10. Дерево решений
        diagram_paths['decision_tree'] = visualize_decision_tree(solver)
        
        logger.info(f"✓ Все визуализации созданы ({len(diagram_paths)} файлов)")
        return diagram_paths
    except Exception as e:
        logger.error(f"Ошибка при генерации визуализаций: {e}")
        raise


def generate_markdown_report(solver: DynamicProgrammingSolver,
                            initial_portfolio: Portfolio,
                            scenarios: Dict[int, List[Scenario]],
                            commissions: Dict[str, float],
                            path: List[Portfolio],
                            actions: List[Tuple[float, float, float]],
                            total_value: float,
                            mc_results: Dict[str, Any],
                            sensitivity_results: Dict[str, List[Dict[str, float]]],
                            criteria_results: Dict[str, float],
                            diagram_paths: Dict[str, str],
                            timing_data: Dict[int, float] = None) -> str:
    """
    Генерирует полный Markdown отчет
    
    Args:
        solver: Решатель
        initial_portfolio: Начальный портфель
        scenarios: Сценарии
        commissions: Комиссии
        path: Траектория портфеля
        actions: Оптимальные действия
        total_value: Максимальный ожидаемый доход
        mc_results: Результаты Monte Carlo
        sensitivity_results: Результаты анализа чувствительности
        criteria_results: Результаты сравнения критериев
        diagram_paths: Пути к диаграммам
        timing_data: Данные о времени выполнения
        
    Returns:
        Путь к созданному отчету
    """
    logger.info("Генерация Markdown отчета...")
    
    filepath = Path('output') / 'report.md'
    
    try:
        report = f"""# Решение задачи динамического программирования: Оптимальное управление инвестиционным портфелем

## 📋 Оглавление

1. [Введение](#введение)
2. [Описание задачи](#описание-задачи)
3. [Этап 1: Инициализация](#этап-1-инициализация)
4. [Этап 2: Сценарии](#этап-2-сценарии)
5. [Этап 3: Динамическое программирование](#этап-3-динамическое-программирование)
6. [Этап 4: Оптимальные решения](#этап-4-оптимальные-решения)
7. [Этап 5: Эволюция портфеля](#этап-5-эволюция-портфеля)
8. [Этап 6: Monte Carlo валидация](#этап-6-monte-carlo-валидация)
9. [Этап 7: Анализ чувствительности](#этап-7-анализ-чувствительности)
10. [Этап 8: Сравнение критериев](#этап-8-сравнение-критериев)
11. [Этап 9: 3D визуализация](#этап-9-3d-визуализация)
12. [Этап 10: Дерево решений](#этап-10-дерево-решений)
13. [Заключение](#заключение)
14. [Код решения](#код-решения)

---

## Введение

Данная работа представляет решение задачи оптимального управления инвестиционным портфелем методом динамического программирования. Задача имеет стохастический характер, так как будущие доходы зависят от вероятностных сценариев развития рынка.

**Цель работы:** Найти оптимальную стратегию управления портфелем, максимизирующую ожидаемый доход за 3 этапа планирования.

**Метод решения:** Динамическое программирование с критерием Байеса (максимизация математического ожидания дохода).

---

## Описание задачи

### 2.1 Постановка задачи

Решить задачу оптимального управления инвестиционным портфелем с использованием метода динамического программирования. Портфель состоит из двух видов ценных бумаг (ЦБ1 и ЦБ2) и депозитов. Управление осуществляется путем покупки/продажи активов пакетами (25% от начального объема).

**Период планирования:** 3 этапа  
**Критерий оптимальности:** Максимизация ожидаемого дохода (критерий Байеса)  
**Особенности:** Учет комиссий брокеров и ограничений на минимальный объем активов

### 2.2 Исходные данные

| Параметр | Значение |
|----------|----------|
| Начальная стоимость ЦБ1 | {INITIAL_CB1:.2f} д.е. |
| Начальная стоимость ЦБ2 | {INITIAL_CB2:.2f} д.е. |
| Начальная стоимость Депозитов | {INITIAL_DEP:.2f} д.е. |
| Начальная касса | {INITIAL_CASH:.2f} д.е. |
| **Итого начальный капитал** | **{initial_portfolio.total_value():.2f} д.е.** |
| Комиссия ЦБ1 | {commissions['cb1']:.1%} {'(включена)' if USE_COMMISSIONS else '(выключена)'} |
| Комиссия ЦБ2 | {commissions['cb2']:.1%} {'(включена)' if USE_COMMISSIONS else '(выключена)'} |
| Комиссия Депозитов | {commissions['dep']:.1%} {'(включена)' if USE_COMMISSIONS else '(выключена)'} |
| Минимум ЦБ1 | {MIN_CB1:.2f} д.е. {'(включен)' if USE_MIN_CONSTRAINTS else '(выключен)'} |
| Минимум ЦБ2 | {MIN_CB2:.2f} д.е. {'(включен)' if USE_MIN_CONSTRAINTS else '(выключен)'} |
| Минимум Депозитов | {MIN_DEP:.2f} д.е. {'(включен)' if USE_MIN_CONSTRAINTS else '(выключен)'} |

### 2.3 Сценарии развития

На каждом этапе возможны три сценария: благоприятный, нейтральный и негативный. Вероятности и мультипликаторы для каждого этапа представлены в таблице ниже.

"""
        
        # Добавляем таблицу сценариев
        report += "#### ЭТАП 1 (Период 0→1):\n\n"
        report += "| Ситуация | Вероятность | ЦБ1 | ЦБ2 | Деп. |\n"
        report += "|----------|-------------|-----|-----|------|\n"
        for scenario in scenarios[1]:
            report += f"| {scenario.situation} | {scenario.probability:.2f} | {scenario.cb1_multiplier:.2f} | {scenario.cb2_multiplier:.2f} | {scenario.dep_multiplier:.2f} |\n"
        
        report += "\n#### ЭТАП 2 (Период 1→2):\n\n"
        report += "| Ситуация | Вероятность | ЦБ1 | ЦБ2 | Деп. |\n"
        report += "|----------|-------------|-----|-----|------|\n"
        for scenario in scenarios[2]:
            report += f"| {scenario.situation} | {scenario.probability:.2f} | {scenario.cb1_multiplier:.2f} | {scenario.cb2_multiplier:.2f} | {scenario.dep_multiplier:.2f} |\n"
        
        report += "\n#### ЭТАП 3 (Период 2→3):\n\n"
        report += "| Ситуация | Вероятность | ЦБ1 | ЦБ2 | Деп. |\n"
        report += "|----------|-------------|-----|-----|------|\n"
        for scenario in scenarios[3]:
            report += f"| {scenario.situation} | {scenario.probability:.2f} | {scenario.cb1_multiplier:.2f} | {scenario.cb2_multiplier:.2f} | {scenario.dep_multiplier:.2f} |\n"
        
        # Продолжаю генерацию отчета...
        report += f"""

---

## Этап 1: Инициализация

### 1.1 Начальное состояние портфеля

Начальный портфель распределен следующим образом:
- **ЦБ1:** {initial_portfolio.cb1:.2f} д.е. ({initial_portfolio.cb1/initial_portfolio.total_value()*100:.1f}%)
- **ЦБ2:** {initial_portfolio.cb2:.2f} д.е. ({initial_portfolio.cb2/initial_portfolio.total_value()*100:.1f}%)
- **Депозиты:** {initial_portfolio.dep:.2f} д.е. ({initial_portfolio.dep/initial_portfolio.total_value()*100:.1f}%)
- **Касса:** {initial_portfolio.cash:.2f} д.е. ({initial_portfolio.cash/initial_portfolio.total_value()*100:.1f}%)
- **Итого:** {initial_portfolio.total_value():.2f} д.е.

![Начальный портфель](diagrams/01_initial_portfolio.png)

*Рис. 1.1 - Распределение начального портфеля*

### 1.2 Параметры задачи

Все параметры задачи представлены в таблице на графике выше.

---

## Этап 2: Сценарии

### 2.1 Матрица сценариев

![Матрица сценариев](diagrams/02_scenarios_matrix.png)

*Рис. 2.1 - Тепловая карта сценариев и вероятностей*

### 2.2 Дерево сценариев

![Дерево сценариев](diagrams/02_scenario_tree.png)

*Рис. 2.2 - Граф дерева сценариев*

### 2.3 Анализ сценариев

На каждом этапе присутствуют три сценария с различными вероятностями. Сумма вероятностей для каждого этапа равна 1.0, что обеспечивает корректность вероятностной модели.

---

## Этап 3: Динамическое программирование

### 3.1 Математическая модель

Рекуррентное соотношение Беллмана:

```
J_t(s_t) = max_{{u_t in U_t(s_t)}} E_{{omega_t}}[J_{{t+1}}(s_{{t+1}}(s_t, omega_t, u_t))]

J_T(s_T) = total_value(s_T)
```

где:
- `J_t(s_t)` - функция ценности состояния s_t на этапе t
- `u_t` - управление (действие) на этапе t
- `omega_t` - реализованный сценарий на этапе t
- `E_{{omega_t}}[·]` - математическое ожидание по сценариям

### 3.2 Процесс DP

![Процесс DP](diagrams/03_dp_process.png)

*Рис. 3.1 - Процесс обратного прохождения DP*

### 3.3 Статистика вычислений

"""
        
        # Добавляем статистику по этапам
        if timing_data:
            report += "| Этап | Кол-во состояний | Время (сек) | Лучшее значение |\n"
            report += "|------|------------------|-------------|-----------------|\n"
            for stage in sorted(solver.value_function.keys()):
                num_states = len(solver.value_function[stage])
                time_val = timing_data.get(stage, 0)
                best_value = max(solver.value_function[stage].values()) if solver.value_function[stage] else 0
                report += f"| {stage} | {num_states} | {time_val:.2f} | {best_value:.2f} д.е. |\n"
        else:
            report += "| Этап | Кол-во состояний | Лучшее значение |\n"
            report += "|------|------------------|-----------------|\n"
            for stage in sorted(solver.value_function.keys()):
                num_states = len(solver.value_function[stage])
                best_value = max(solver.value_function[stage].values()) if solver.value_function[stage] else 0
                report += f"| {stage} | {num_states} | {best_value:.2f} д.е. |\n"
        
        report += f"""

---

## Этап 4: Оптимальные решения

### 4.1 Оптимальные действия

![Оптимальные действия](diagrams/04_optimal_actions.png)

*Рис. 4.1 - Оптимальные действия на каждом этапе*

### 4.2 Таблица решений

| Этап | ЦБ1 (д.е.) | ЦБ2 (д.е.) | Деп (д.е.) | Ожидаемый доход |
|------|------------|------------|------------|----------------|
"""
        
        for i, action in enumerate(actions, 1):
            delta_cb1, delta_cb2, delta_dep = action
            expected_val = solver.value_function[1].get(
                solver._find_closest_state(initial_portfolio, 1), total_value
            ) if i == 1 else 0
            report += f"| {i} | {delta_cb1:+.2f} | {delta_cb2:+.2f} | {delta_dep:+.2f} | {expected_val:.2f} д.е. |\n"
        
        report += f"""

### 4.3 Интерпретация решений

Оптимальная стратегия показывает, какие действия необходимо предпринять на каждом этапе для максимизации ожидаемого дохода. Положительные значения означают покупку, отрицательные - продажу.

---

## Этап 5: Эволюция портфеля

### 5.1 Изменение портфеля

![Эволюция портфеля](diagrams/05_portfolio_evolution.png)

*Рис. 5.1 - Эволюция портфеля по этапам*

### 5.2 Поэтапное описание

"""
        
        # Добавляем описание каждого этапа
        for i, portfolio in enumerate(path):
            if i % 2 == 0:
                stage_num = i // 2
                report += f"**Этап {stage_num} (состояние):**\n\n"
                report += f"- ЦБ1: {portfolio.cb1:.2f} д.е.\n"
                report += f"- ЦБ2: {portfolio.cb2:.2f} д.е.\n"
                report += f"- Депозиты: {portfolio.dep:.2f} д.е.\n"
                report += f"- Касса: {portfolio.cash:.2f} д.е.\n"
                report += f"- **Итого: {portfolio.total_value():.2f} д.е.**\n\n"
        
        report += f"""

---

## Этап 6: Monte Carlo валидация

### 6.1 Результаты симуляции

![MC распределение](diagrams/06_monte_carlo_distribution.png)

*Рис. 6.1 - Распределение доходов при 5000 симуляциях*

### 6.2 CDF (Cumulative Distribution Function)

![MC CDF](diagrams/06_monte_carlo_cdf.png)

*Рис. 6.2 - Кумулятивное распределение доходов*

### 6.3 Статистика результатов

| Метрика | Значение |
|---------|----------|
| Ожидание (E) | {mc_results.get('mean', 0):.2f} д.е. |
| Стд.откл (σ) | {mc_results.get('std', 0):.2f} д.е. |
| Минимум | {mc_results.get('min', 0):.2f} д.е. |
| Максимум | {mc_results.get('max', 0):.2f} д.е. |

### 6.4 Выводы

Monte Carlo симуляция подтверждает корректность решения. Ожидаемое значение близко к результату динамического программирования.

---

## Этап 7: Анализ чувствительности

### 7.1 Чувствительность к вероятностям

![Чувствительность к вероятностям](diagrams/07_sensitivity_probabilities.png)

*Рис. 7.1 - Влияние изменения вероятностей на доход*

### 7.2 Чувствительность к комиссиям

![Чувствительность к комиссиям](diagrams/07_sensitivity_commissions.png)

*Рис. 7.2 - Влияние изменения комиссий на доход*

### 7.3 Интерпретация

Анализ чувствительности показывает, насколько устойчиво решение к изменениям параметров задачи.

---

## Этап 8: Сравнение критериев

### 8.1 Сравнение критериев принятия решений

![Сравнение критериев](diagrams/08_criteria_comparison.png)

*Рис. 8.1 - Сравнение результатов для разных критериев*

### 8.2 Таблица сравнения

| Критерий | Оптимальный доход (д.е.) | Относительно Байеса |
|----------|--------------------------|---------------------|
"""
        
        bayesian_value = criteria_results.get('Байес', total_value)
        for criterion, value in criteria_results.items():
            relative = ((value - bayesian_value) / bayesian_value * 100) if bayesian_value > 0 else 0
            report += f"| {criterion} | {value:.2f} | {relative:+.2f}% |\n"
        
        report += f"""

---

## Этап 9: 3D визуализация

### 9.1 3D поверхность функции ценности

![3D функция ценности](diagrams/09_3d_value_function.png)

*Рис. 9.1 - Трехмерная визуализация функции ценности*

### 9.2 Анализ оптимальной области

3D визуализация показывает зависимость максимального дохода от распределения активов в портфеле.

---

## Этап 10: Дерево решений

### 10.1 Полное дерево решений

![Дерево решений](diagrams/10_decision_tree.png)

*Рис. 10.1 - Граф дерева решений со всеми состояниями и действиями*

### 10.2 Оптимальный путь

Оптимальный путь через дерево решений выделен и показывает последовательность состояний и действий, ведущих к максимальному ожидаемому доходу.

---

## Заключение

### Основные результаты:

1. **Максимальный ожидаемый доход:** {total_value:.2f} д.е.
2. **Оптимальные действия:** 
"""
        
        for i, action in enumerate(actions, 1):
            delta_cb1, delta_cb2, delta_dep = action
            report += f"   - Этап {i}: ЦБ1={delta_cb1:+.2f}, ЦБ2={delta_cb2:+.2f}, Деп={delta_dep:+.2f}\n"
        
        report += f"""
3. **Риск:** Стандартное отклонение = {mc_results.get('std', 0):.2f} д.е.
4. **Стабильность:** Monte Carlo валидация подтверждает корректность решения

### Выводы:

1. Алгоритм динамического программирования успешно находит оптимальную стратегию управления портфелем.
2. Учет комиссий {'существенно влияет' if USE_COMMISSIONS else 'не влияет'} на оптимальные решения.
3. Ограничения на минимальный объем {'ограничивают' if USE_MIN_CONSTRAINTS else 'не ограничивают'} пространство допустимых решений.
4. Стохастический характер задачи требует использования вероятностных критериев.

### Что дальше:

Возможные улучшения:
- Увеличение количества этапов планирования
- Более детальная дискретизация пространства состояний
- Учет транзакционных издержек
- Многоцелевая оптимизация (доход vs риск)

---

## Код решения

### Файловая структура проекта

\`\`\`
lab4/
├── constants.py              # Константы
├── models.py                 # Модели данных
├── data_loader.py            # Загрузка данных
├── solver.py                 # Решатель DP
├── path_recovery.py          # Восстановление пути
├── visualizers.py            # Визуализация
├── data_exporter.py          # Экспорт данных
├── sensitivity_analysis.py   # Анализ чувствительности
├── criteria_comparison.py     # Сравнение критериев
├── generate_report.py        # Генерация отчета
├── main.py                   # Главный скрипт
└── output/                   # Результаты
    ├── report.md            # Отчет
    ├── diagrams/            # Графики
    └── data/               # CSV данные
\`\`\`

### Запуск программы

\`\`\`bash
python main.py
\`\`\`

или для генерации полного отчета:

\`\`\`bash
python generate_report.py
\`\`\`

---

**Дата создания:** {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}  
**Автор:** Лабораторная работа по методам оптимизации  
**Версия:** 1.0  
**Статус:** Завершено ✅
"""
        
        with open(filepath, 'w', encoding='utf-8') as f:
            f.write(report)
        
        logger.info(f"✓ Отчет сохранен: {filepath}")
        return str(filepath)
    except Exception as e:
        logger.error(f"Ошибка при генерации отчета: {e}")
        raise


def create_readme() -> str:
    """
    Создает/обновляет README.md для GitHub
    
    Returns:
        Путь к README.md
    """
    filepath = Path('README.md')
    
    try:
        readme_content = f"""# Dynamic Programming Portfolio Optimization

## Описание

Решение задачи оптимального управления инвестиционным портфелем с использованием метода динамического программирования. Портфель состоит из двух видов ценных бумаг и депозитов. Управление осуществляется на 3 этапах с учетом стохастических сценариев развития рынка.

## Результаты

- **Максимальный ожидаемый доход:** Результаты в [output/report.md](output/report.md)
- **Статистика:** Monte Carlo валидация (5000 симуляций)
- **Визуализация:** 10 типов графиков и диаграмм

## Содержание

- `output/report.md` - Полный отчет с графиками и анализом
- `output/diagrams/` - Все диаграммы и графики (PNG, 150 DPI)
- `output/data/` - CSV файлы с данными для воспроизводимости
- Исходный код в корневой директории

## Быстрый старт

### Установка зависимостей

\`\`\`bash
pip install -r requirements.txt
\`\`\`

### Запуск решения

\`\`\`bash
python main.py
\`\`\`

### Генерация полного отчета

\`\`\`bash
python generate_report.py
\`\`\`

## Структура проекта

\`\`\`
lab4/
├── constants.py              # Константы и настройки
├── models.py                 # Классы Portfolio, Scenario, Decision
├── data_loader.py            # Загрузка данных из Excel
├── solver.py                 # Решатель динамического программирования
├── path_recovery.py          # Восстановление оптимальной траектории
├── visualizers.py            # Расширенная визуализация (10 функций)
├── data_exporter.py          # Экспорт данных в CSV
├── sensitivity_analysis.py   # Анализ чувствительности
├── criteria_comparison.py     # Сравнение критериев принятия решений
├── generate_report.py        # Генерация Markdown отчета
├── main.py                   # Главный скрипт
├── test_commissions.py       # Тесты
├── requirements.txt          # Зависимости
└── output/                   # Результаты
    ├── report.md            # Полный отчет
    ├── diagrams/            # Графики (PNG)
    └── data/                # CSV данные
\`\`\`

## Особенности

- ✅ Полная реализация алгоритма динамического программирования
- ✅ Учет комиссий брокеров (опционально)
- ✅ Ограничения на минимальный объем активов (опционально)
- ✅ Monte Carlo валидация решения
- ✅ Анализ чувствительности к параметрам
- ✅ Сравнение различных критериев принятия решений
- ✅ Расширенная визуализация (10 типов графиков)
- ✅ Полный Markdown отчет с встроенными изображениями

## Настройки

Настройки можно изменить в файле `constants.py`:

- `USE_COMMISSIONS` - включить/выключить комиссии
- `USE_MIN_CONSTRAINTS` - включить/выключить ограничения на минимум
- `INITIAL_CASH` - начальная касса

## Документация

- [algorithm_pseudocode.md](algorithm_pseudocode.md) - Псевдокод алгоритма
- [class_diagram.md](class_diagram.md) - Диаграмма классов
- [examples.md](examples.md) - Демонстрационные примеры
- [output/report.md](output/report.md) - Полный отчет с результатами

## Лицензия

Лабораторная работа выполнена в рамках курса "Методы оптимизации".

---

*Дата создания: {datetime.now().strftime('%Y-%m-%d')}*  
*Версия: 1.0*
"""
        
        with open(filepath, 'w', encoding='utf-8') as f:
            f.write(readme_content)
        
        logger.info(f"✓ README.md создан/обновлен: {filepath}")
        return str(filepath)
    except Exception as e:
        logger.error(f"Ошибка при создании README: {e}")
        raise


def main():
    """
    Главная функция генерации полного отчета
    """
    print("=" * 70)
    print("ГЕНЕРАЦИЯ ПОЛНОГО ОТЧЕТА С ВИЗУАЛИЗАЦИЕЙ")
    print("=" * 70)
    
    timing_data = {}
    
    try:
        # 1. Создать структуру
        print("\n[1/8] Создание структуры папок...")
        create_report_structure()
        
        # 2. Загрузить данные
        print("\n[2/8] Загрузка данных...")
        excel_file = "Данные для постановки задачи.xlsx"
        import os
        if os.path.exists(excel_file):
            scenarios, commissions, initial_cash = load_from_excel(excel_file)
        else:
            from constants import SCENARIOS, COMMISSIONS, INITIAL_CASH
            scenarios = {}
            for stage, stage_data in SCENARIOS.items():
                scenarios[stage] = [
                    Scenario(
                        situation=item['situation'],
                        probability=item['probability'],
                        cb1_multiplier=item['cb1'],
                        cb2_multiplier=item['cb2'],
                        dep_multiplier=item['dep']
                    )
                    for item in stage_data
                ]
            commissions = COMMISSIONS.copy()
            initial_cash = INITIAL_CASH
        
        validate_scenarios(scenarios)
        initial_portfolio = initialize_portfolio(cash=initial_cash)
        
        # 3. Решить задачу DP
        print("\n[3/8] Решение задачи DP...")
        start_time = time.time()
        solver = DynamicProgrammingSolver(
            initial_portfolio=initial_portfolio,
            scenarios=scenarios,
            commissions=commissions,
            use_commissions=USE_COMMISSIONS,
            use_min_constraints=USE_MIN_CONSTRAINTS
        )
        solver.solve_backward()
        elapsed = time.time() - start_time
        timing_data[1] = elapsed
        path, actions, total_value = solver.get_optimal_path()
        print(f"  ✓ Решение найдено за {elapsed:.2f} сек")
        print(f"  ✓ Максимальный ожидаемый доход: {total_value:.2f} д.е.")
        
        # 4. Monte Carlo валидация
        print("\n[4/8] Monte Carlo валидация (5000 симуляций)...")
        mc_results = monte_carlo_simulation(
            solver, initial_portfolio, scenarios,
            n_simulations=5000, random_seed=42
        )
        print(f"  ✓ Ожидание: {mc_results['mean']:.2f} д.е.")
        
        # 5. Анализ чувствительности
        print("\n[5/8] Анализ чувствительности...")
        sensitivity_results = run_full_sensitivity_analysis(
            initial_portfolio, scenarios, commissions,
            USE_COMMISSIONS, USE_MIN_CONSTRAINTS
        )
        print(f"  ✓ Анализ завершен")
        
        # 6. Сравнение критериев
        print("\n[6/8] Сравнение критериев...")
        criteria_results = compare_all_criteria(
            initial_portfolio, scenarios, commissions,
            USE_COMMISSIONS, USE_MIN_CONSTRAINTS
        )
        print(f"  ✓ Сравнение завершено")
        
        # 7. Генерация визуализаций
        print("\n[7/8] Генерация визуализаций...")
        diagram_paths = generate_all_visualizations(
            solver, initial_portfolio, scenarios, commissions,
            path, actions, mc_results, sensitivity_results, criteria_results, timing_data
        )
        print(f"  ✓ Создано {len(diagram_paths)} графиков")
        
        # 8. Экспорт данных
        print("\n[8/9] Экспорт данных в CSV...")
        expected_values = None
        exported_files = export_all_data(
            actions, path, mc_results, sensitivity_results, expected_values
        )
        print(f"  ✓ Экспортировано {len(exported_files)} CSV файлов")
        
        # 9. Генерация отчета
        print("\n[9/9] Генерация Markdown отчета...")
        report_path = generate_markdown_report(
            solver, initial_portfolio, scenarios, commissions,
            path, actions, total_value, mc_results,
            sensitivity_results, criteria_results, diagram_paths, timing_data
        )
        print(f"  ✓ Отчет создан: {report_path}")
        
        # 10. Создание README
        print("\n[10/10] Создание README...")
        readme_path = create_readme()
        print(f"  ✓ README создан: {readme_path}")
        
        print("\n" + "=" * 70)
        print("✅ ПОЛНЫЙ ОТЧЕТ УСПЕШНО СОЗДАН!")
        print("=" * 70)
        print("\nЛокация файлов:")
        print(f"  📄 {report_path} - основной отчет")
        print(f"  📊 output/diagrams/ - все графики (PNG)")
        print(f"  📈 output/data/ - данные в CSV")
        print(f"  📖 {readme_path} - описание проекта для GitHub")
        
    except Exception as e:
        logger.error(f"Критическая ошибка при генерации отчета: {e}")
        import traceback
        traceback.print_exc()
        raise


if __name__ == "__main__":
    main()
